const validateBody = require("./validateBody");
const isValidId = require("./isValidId");
const isFavoriteValid = require("./isFavoriteValid");

module.exports = { validateBody, isValidId, isFavoriteValid };
